version: "3"

services:

  fpsandboc:
    image: geerlingguy/drupal-vm
    container_name: fpsandbox
    ports:
      - 8080:80
      - 443:443
    privileged: true
    volumes:
      - /var/lib/mysql
    command: /lib/systemd/systemd
